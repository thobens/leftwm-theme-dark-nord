#!/bin/sh
SCRIPTPATH="$( cd "$(dirname "$0")" ; pwd -P )"

#set background
feh --bg-scale "$SCRIPTPATH"/down.jpg

eww close-all -c $SCRIPTPATH/.config/eww/dark-nord 
eww kill -c $SCRIPTPATH/.config/eww/dark-nord

pkill eww

leftwm command "UnloadTheme"

pkill picom
# pkill polybar
pkill dunst
